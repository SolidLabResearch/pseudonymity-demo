export interface ClientCredentials {
  id: string;
  secret: string;
}

export interface CssUserConfig {
  podName: string
  email: string
  password: string
  createWebId: boolean
  createPod: boolean
  register: boolean
  webId: string
  css: string
}
